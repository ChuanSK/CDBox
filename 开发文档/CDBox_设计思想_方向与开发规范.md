# CDBox 设计思想、产品方向与开发规范

> **文档性质：持续维护的最高级设计规范（Living Specification）**  
> **适用对象：Codex、后续开发者、代码审查与版本规划**  
> **当前版本：1.0.0**  
> **建立日期：2026-07-15**  
> **维护方式：后续在当前聊天中形成的新结论，应持续合并到本文档，并在“变更记录”中登记。**

---

## 0. 文档用途与执行优先级

本文档用于统一 CDBox 后续开发中的产品定位、交互方式、数据模型、兼容性、代码架构和实施顺序，避免 Codex 在单次任务中只关注局部需求而破坏既有逻辑或长期方向。

Codex 修改现有代码或开发新功能时，必须按以下优先级处理约束：

1. 用户在当前任务中明确提出的最新要求；
2. 本文档中的“强制原则”和“禁止事项”；
3. 用户指定的最新可用源码及其现有稳定实现；
4. 本文档中的推荐架构、交互和实施路线；
5. 代码整洁、抽象程度和技术偏好。

当局部需求与本文档冲突时，不得自行选择其一。Codex 应先指出冲突、说明影响，并给出兼容方案。

本文档不是要求一次性重构整个 CDBox。所有改造均应遵循：

> **先调查现状，复用稳定逻辑，小步实施，可测试，可回退。**

---

# 第一部分：产品定位与总体设计思想

## 1. CDBox 的产品定位

CDBox 不应只是不断增加命令的“工具箱”，也不应成为只能依赖自身运行、脱离普通 CAD 就无法使用的封闭专业软件。

CDBox 的长期定位是：

> **基于标准 DWG 和 AutoCAD/CASS 原生图元，为管线测绘提供属性管理、对象关联、标注、断面、工程量、检查和智能辅助能力的 CAD 增强层。**

其理想工作方式是：

```text
标准 CAD 图元
    +
CDBox 附加语义与关联数据
    +
面向管线测绘的专业工作流
```

CDBox 的核心竞争力不是“只有安装 CDBox 才能看到图”，而是：

- 图纸始终可交付；
- 普通 CAD 始终可查看和编辑；
- 安装 CDBox 后获得更高效的整体编辑、关联刷新、检查和统计能力；
- 用户仍掌握最终制图决定；
- 插件负责减少重复劳动、维护数据一致性并发现问题。

## 2. CDBox 的核心产品原则

### 2.1 CDBox 可以增强 CAD，但不能绑架 CAD

所有 CDBox 生成对象必须优先保证普通 CAD 兼容性。不得为了实现智能能力而使图纸依赖插件才能正常显示或编辑。

### 2.2 专业化不等于自动化一切

市政管线图中，主管、支管、节点、道路、地物和既有注记高度密集。标注位置不仅是几何避让问题，还涉及制图习惯、信息优先级和人工判断。

因此 CDBox 不应将“全自动标注和完美自动避让”作为近期核心目标。更合理的目标是：

> **让人工标注和调整更快、更稳定、更容易维护。**

### 2.3 对象全生命周期优先于功能数量

一条管线或一个节点应贯穿完整业务流程：

```text
创建或识别
→ 属性编辑
→ 节点和拓扑关联
→ 标注
→ 断面
→ 工程量
→ 属性修改
→ 关联刷新
→ 检查
→ 出图
```

后续开发应优先打通已有模块之间的数据和操作链路，而不是持续增加相互独立的新窗口和新命令。

### 2.4 确定性算法优先，AI 作为辅助层

长度、连接关系、工程量、属性联动和对象关联等核心逻辑必须由明确、可验证、可撤销的程序规则完成。

AI 适合用于：

- 将自然语言转换为明确命令；
- 汇总和解释检查结果；
- 推荐修复方法；
- 帮助生成规则和模板；
- 辅助用户理解图纸问题。

AI 不应在缺少确定性验证和撤销机制时直接大规模修改图纸，也不应成为核心工程量或几何判断的唯一依据。

---

# 第二部分：原生兼容性与图纸交付规范

## 3. 原生 CAD 兼容是最高级技术约束

CDBox 必须避免类似部分行业软件的封闭对象问题。由 CDBox 编辑或生成的图纸，在未安装 CDBox 的 AutoCAD/CASS 中也必须尽可能满足：

- 正常打开；
- 正常显示；
- 正常选择；
- 正常移动；
- 正常删除；
- 正常修改文字、线、块、填充和其他标准图元；
- 正常复制、粘贴和另存；
- 正常打印与导出 PDF；
- 不出现必须安装对象使能器才能显示的私有对象；
- 不出现大面积代理图形、空白图形或不可编辑对象。

## 4. 禁止采用的对象路线

除非用户后续明确推翻本规范，禁止采用以下方案作为 CDBox 核心图形对象：

- ObjectARX 私有实体；
- 自定义代理对象；
- 必须安装 CDBox 才能显示的管线、节点、断面或标注实体；
- 将外部数据库作为图纸显示和基础编辑的必要条件；
- 保存后只能通过 CDBox 专用界面修改的不可拆分黑盒对象。

## 5. 推荐的数据承载路线

### 5.1 图形使用标准 AutoCAD 图元

推荐对应关系：

| 业务对象 | 推荐图形载体 |
|---|---|
| 管线 | `Polyline`、`Line` 等标准线实体 |
| 节点 | 标准块参照、圆、文字等 |
| 标注 | `DBText`、`MText`、`Line`、`Polyline`、`Leader`、`MLeader` 等，以现有稳定实现为准 |
| 断面 | `Line`、`Polyline`、`Circle`、`Hatch`、`DBText`、标注实体 |
| 图框 | 标准块、线、文字 |
| 裁图 | AutoCAD 原生布局、视口或标准图元方案 |

Codex 不得仅为了“代码更统一”而随意替换已经稳定的图元类型。例如现有功能已经确认使用 `DBText` 且效果正确，则不得未经验证改为 `MText`。

### 5.2 智能能力使用附加数据

可使用：

- XData；
- Extension Dictionary；
- XRecord；
- AutoCAD 原生 Group；
- 块属性或其他标准 DWG 内部数据结构。

附加数据丢失或损坏时，图形本身仍必须能够正常显示和手工编辑。

### 5.3 外部项目数据库只能作为增强层

未来可使用 SQLite 或项目数据库保存：

- 跨图纸项目索引；
- 统计缓存；
- 历史记录；
- 检查结果；
- 项目模板；
- 大型查询优化数据。

但必须满足：

- 没有数据库时 DWG 仍可正常打开和基础编辑；
- 关键关联数据应尽量保留在 DWG 中；
- 外部索引可由 DWG 重新扫描生成；
- 不允许形成“数据库丢失等于图纸报废”的依赖。

---

# 第三部分：智能标注系统设计规范

## 6. 标注系统的真实问题与优化目标

当前标注功能已经能够生成正确内容并提供较好的预览，但复杂图面中，用户仍需频繁：

1. 拖动文字；
2. 单独拖动横线；
3. 单独调整斜引线；
4. 检查组成图元是否错位；
5. 调整失败后删除并重新标注。

支管密集区域的空间不足决定了程序无法稳定替用户完成最终排版。因此近期优化目标不是“自动找到完美位置”，而是：

> **将标注从多个难以维护的独立图元，升级为普通 CAD 可编辑、CDBox 可整体管理的智能标注。**

## 7. 智能标注的推荐组成

### 7.1 标准图元 + 原生 Group + 元数据

同一标注可由以下内容构成：

```text
CDBox 逻辑标注
├── 主文字
├── 次文字或开挖信息
├── 横线
├── 斜引线
└── 其他必要标准图元
```

推荐：

- 各部分继续使用现有标准图元；
- 使用 AutoCAD 原生 `Group` 建立普通 CAD 中可整体选择的逻辑组合；
- 使用 XData 或扩展字典记录 CDBox 标注 ID、类型、组成部分和关联源对象；
- 即使 Group 被解除，CDBox 仍可根据标注 ID 尝试恢复组成关系。

最终采用 Group、块或其他原生结构前，Codex 必须先检查最新源码中三类标注的实际构成，不得直接重写已有稳定落图逻辑。

### 7.2 稳定 CDBox 对象 ID

仅依赖 AutoCAD Handle 不足以支持复制、分割、合并和跨图操作。建议逐步为 CDBox 管理对象写入稳定 ID：

```text
CDBoxObjectId = GUID
CDBoxObjectType = Pipe / Node / Area / Annotation
MetadataVersion = 1
```

标注元数据示例：

```text
AnnotationId = GUID
AnnotationType = PipeLength / Node / SurfaceArea
SourceObjectIds = [...]
ContentMode = Auto / ManualOverride
MetadataVersion = 1
```

每个子图元保存最小信息：

```text
AnnotationId = GUID
AnnotationPart = MainText / SecondaryText / LandingLine / LeaderLine
```

### 7.3 普通 CAD 与 CDBox 环境下的双层体验

在普通 CAD 中：

- 标注各部分可正常显示；
- 可整体移动原生 Group；
- 可解除组；
- 可单独修改文字、引线和线段；
- 可删除或重新组合。

在安装 CDBox 后：

- 选择任一组成部分可识别完整标注；
- 可整体移动并保持几何关系；
- 可定位关联源对象；
- 可刷新内容；
- 可检查标注是否失效；
- 可修复被破坏的组成关系。

## 8. 智能标注交互规范

### 8.1 整体移动优先

用户移动标注时，应以一个逻辑整体进行：

- 文字、横线和引线同步变化；
- 引线端点保持在关联对象或用户指定锚点上；
- 横线长度根据文字重新计算；
- 不需要用户分别拖动多个组成对象。

### 8.2 编辑入口

可按阶段提供：

1. 显式命令选择标注进入编辑；
2. 右键菜单“编辑 CDBox 标注”；
3. 双击标注；
4. 侧栏或上下文面板编辑。

初期优先显式命令，确认稳定后再加入双击事件，避免事件拦截、焦点和多文档环境问题。

### 8.3 编辑能力

标注编辑可包含：

- 重新选择关联对象；
- 重新选择标注位置；
- 切换引线方向；
- 调整文字对齐；
- 调整精确位数；
- 控制是否显示某类附加内容；
- 恢复自动生成内容；
- 保留或取消人工覆盖。

### 8.4 自动内容与手工内容冲突

必须区分：

- 根据源对象自动生成的内容；
- 用户明确手工修改的内容；
- 源对象当前真实属性。

推荐保存：

```text
ContentMode = Auto / ManualOverride
LastGeneratedSignature = ...
LastSourceSignature = ...
```

刷新规则：

- `Auto`：允许直接重新生成；
- `ManualOverride`：发现差异时提示用户；
- 禁止静默覆盖用户明确修改的文字；
- 应支持“恢复自动”操作。

## 9. 标注刷新策略

第一阶段不建议实现所有属性变化后的实时自动更新。原因：

- AutoCAD 事件和事务嵌套复杂；
- 容易破坏撤销、重做；
- 大图可能产生性能问题；
- 用户手工内容可能被意外覆盖；
- 多对象连续修改时可能重复刷新。

推荐阶段路线：

1. 单标注明确刷新；
2. 选择范围刷新；
3. 全图标注检查并提示差异；
4. 可选批量刷新；
5. 后续再评估防抖、延迟、可配置的事件驱动更新。

## 10. 标注检查系统

标注对象化后，优先发展检查而不是全自动排版。

### 10.1 关联检查

- 对象存在但缺少标注；
- 标注关联对象已删除；
- 标注关联对象类型错误；
- 同一对象存在重复标注；
- 标注 Group 丢失；
- 标注子图元缺失；
- 子图元标注 ID 不一致；
- 复制后出现重复 CDBox ID。

### 10.2 内容检查

- 标注长度与当前几何长度不一致；
- 规格、材质、恢复类型与对象属性不一致；
- 节点编号、井深和井类型不一致；
- 精确位数和格式不符合当前设置；
- 标注仍使用旧属性。

### 10.3 图面检查

可逐步增加：

- 文字框重叠；
- 文字压线；
- 引线穿过其他文字；
- 引线过长；
- 标注间距过小；
- 候选位置提示。

第一目标是发现并定位问题，而不是无条件自动移动全部标注。

## 11. 自动避让和自动标注的边界

### 11.1 暂不作为近期核心目标

复杂管线图中的标注布局接近 GIS 标签排布和二维组合优化问题。即使投入大量开发，也难以消除人工调整。

### 11.2 可做的有限自动化

后续只优先实现确定性较高的辅助：

- 低密度主管的候选位置；
- 同方向标注对齐；
- 固定间距排列；
- 支管标注分组；
- 重叠检测；
- 提供多个位置候选，由用户确认；
- 对简单区域进行批量初始标注。

不得以“自动避让”名义大范围重写当前稳定的预览和人工选点流程。

---

# 第四部分：用户体验与界面体系规范

## 12. 当前大型工作台的问题

工作台作为全部功能的融合主体，在实际绘图时会出现：

- 界面面积较大；
- 遮挡主要绘图区域；
- 高频简单操作被迫进入大型界面；
- 用户需要频繁在绘图区和工作台间切换；
- 工作台承担了管理、设置、执行、对象编辑等过多职责。

问题不应仅通过“继续缩小工作台”解决，而应重新划分不同界面的职责。

## 13. 推荐的多层界面体系

CDBox 后续应采用：

> **紧凑侧栏 + CAD 命令 + 临时上下文面板 + 按需独立页面 + 大型工作台管理中心。**

### 13.1 CAD 命令：最快执行入口

适合：

- 立即开始标注；
- 选择对象；
- 生成断面；
- 属性清除；
- 快速刷新；
- 检查当前对象。

原则：

- 输入命令后直接进入操作；
- 不应先打开大型页面；
- 设置应通过独立设置入口调整；
- 命令行仍是专业用户最高效的入口之一。

### 13.2 紧凑停靠侧栏：日常对象操作中心

建议使用 AutoCAD `PaletteSet` 停靠在 CAD 左侧或右侧。

建议特性：

- 展开宽度约 280～360 px；
- 折叠宽度约 44～60 px；
- 可固定、自动隐藏和关闭；
- 默认不强制展开；
- 不复制完整工作台；
- 根据当前选择动态显示内容。

侧栏只承担四类内容：

1. 当前对象摘要；
2. 当前对象最相关的快捷操作；
3. 收藏和最近使用；
4. 问题或状态摘要。

选择管线时示例：

```text
当前对象：主管
规格：110PVC
长度：12.35m
起点：J01
终点：J02

[编辑属性] [管长标注]
[生成断面] [调换起终点]
[刷新标注] [检查对象]
```

选择不同对象时，应显示不同动作，不应固定列出全部功能。

### 13.3 临时上下文面板：当前操作编辑器

适合：

- 双击标注后的快速调整；
- 管线属性少量修改；
- 重新选点；
- 引线方向切换；
- 标注刷新；
- 快速查看关联对象。

要求：

- 面积小；
- 不长期遮挡；
- `Esc` 可关闭；
- 操作完成后可自动消失；
- 只放高频字段；
- 复杂设置跳转到独立页面。

### 13.4 独立页面：复杂业务界面

适合：

- 属性识别表；
- 属性默认表；
- 标注设置；
- 工程量仪表盘；
- 结构层复杂编辑；
- 检查报告；
- 更新、日志和诊断；
- 批量管理。

要求：

- 只在用户主动打开时出现；
- 记忆窗口位置和大小；
- 可移动至第二显示器；
- 不强制置顶；
- 重复打开时复用已有窗口；
- 关闭后不影响核心 CAD 命令。

### 13.5 大型工作台：管理中心而非日常必经入口

大型 Studio 工作台应继续保留，但重新定位为：

- 系统管理中心；
- 项目概览；
- 功能导航；
- 批量管理入口；
- 检查中心；
- 设置和更新中心；
- 新用户学习入口。

不得要求用户为了执行一个高频绘图命令而长期打开工作台。

## 14. 同一功能的多入口职责

同一业务能力可有多个入口，但不能简单复制同一套大界面：

| 入口 | 主要职责 |
|---|---|
| 命令 | 立即执行 |
| 侧栏 | 查看当前对象、快速执行和少量编辑 |
| 上下文面板 | 当前对象或当前标注的临时调整 |
| 独立页面 | 完整设置、复杂编辑和批量操作 |
| 工作台 | 管理、导航、概览和检查 |

## 15. 专注绘图模式

可增加可选“专注绘图模式”：

- CDBox 交互命令开始时收起大型工作台；
- 侧栏可自动折叠；
- 只保留必要预览和状态；
- 普通提示不弹大型窗口；
- 命令完成后恢复之前状态。

不得监听所有 CAD 命令并频繁隐藏界面，只应针对 CDBox 自身的绘图交互命令。

## 16. WebView2 使用规范

### 16.1 控制实例数量

不应为每个小面板创建一个新的 WebView2 实例。建议长期复用：

- 一个侧栏 WebView2；
- 一个大型工作台或通用独立页宿主；
- 临时小面板优先使用轻量 WinForms/WPF，或复用专用宿主。

### 16.2 页面按需加载

- 启动时不加载全部业务页面；
- 侧栏只加载当前需要的组件；
- 复杂页面首次打开时再初始化；
- 避免隐藏页面仍持续刷新。

### 16.3 焦点规则

WebView2 不得长期抢占：

- `Esc`；
- 空格；
- Delete；
- CAD 快捷命令键盘输入；
- Ctrl+C / Ctrl+V。

用户点击绘图区后，焦点必须可靠返回 CAD 文档窗口。

---

# 第五部分：对象模型与代码架构规范

## 17. 从功能模块转向统一对象模型

当前不同功能可能分别识别和处理“管线”“节点”“结构层”和“标注”。后续应逐步建立统一领域对象，避免：

- 属性编辑器有一套管线解析；
- 标注功能又有一套管线解析；
- 工程量功能再有一套管线解析；
- 同一公式在不同模块重复实现；
- 修改一个联动规则时遗漏其他模块。

推荐领域模型示例：

```text
CDPipe
- Id
- CadEntityId / Handle
- Geometry
- ParentProperty
- Category
- Tags
- Diameter
- Material
- StartNodeId
- EndNodeId
- StructureLayers
- AnnotationIds

CDNode
- Id
- CadEntityId / Handle
- NodeType
- Number
- Depth
- ConnectedPipeIds
- AnnotationIds

CDAnnotation
- Id
- Type
- SourceObjectIds
- PartEntityIds
- GroupId
- ContentMode
- Style
- MetadataVersion
```

领域模型不应直接绑定某个 WinForms 控件或 WebView2 页面。

## 18. 推荐分层

可逐步形成以下逻辑分层，不要求一次拆为多个程序集：

```text
CDBox.Core
- 配置
- 日志
- 版本
- 错误模型
- ID 与元数据版本

CDBox.Cad
- AutoCAD API 封装
- 事务
- 选择集
- 图元读写
- Group 管理
- XData / Extension Dictionary
- 高亮与定位

CDBox.Domain
- Pipe / Node / Annotation 等领域模型
- 纯业务规则
- 不依赖 UI

CDBox.Metadata
- CDBox ID
- 关联数据读写
- 数据迁移
- 重复 ID 检测和修复

CDBox.Annotation
- 标注创建
- 标注编辑
- 整体移动
- 标注刷新
- 标注检查
- 各标注类型策略

CDBox.Quantity
- 工程量计算

CDBox.Validation
- 几何检查
- 属性检查
- 标注检查
- 出图检查

CDBox.UI.WinForms
- 旧界面和兜底界面

CDBox.Studio
- WebView2 页面
- 工作台
- 侧栏
- 独立页宿主

CDBox.Update
- 更新检查
- 下载
- 安装
- 回滚
- 诊断
```

## 19. CAD API 隔离

业务代码不应在大量位置直接创建 Transaction、访问 Database、修改图元和处理文档锁。

建议逐步抽象：

```text
ICadTransactionRunner
ICadSelectionService
ICadEntityStore
IObjectMetadataStore
IAnnotationEntityStore
ICadHighlightService
ICadDocumentContext
```

目标：

- 统一事务使用方式；
- 降低 AutoCAD 版本升级影响；
- 降低业务逻辑和 UI 对 AutoCAD 类的直接依赖；
- 便于测试纯业务规则；
- 为未来兼容其他 CAD 平台保留可能性。

不得为了追求“完美架构”一次性重写所有稳定代码。应先在新模块和高风险重复逻辑处建立边界，再逐步迁移。

## 20. UI 宿主架构

业务页面不应硬编码为“只能内嵌工作台”或“只能独立窗口”。建议建立通用宿主概念：

```text
CDBoxPage
├── CompactPaletteHost
├── StudioEmbeddedHost
├── StandaloneWindowHost
└── ContextPopupHost
```

页面声明支持的宿主模式：

```csharp
public enum PageHostMode
{
    CompactPalette,
    StudioEmbedded,
    Standalone,
    ContextPopup
}
```

同一业务逻辑应复用，允许不同宿主显示不同密度的界面，而不是复制业务代码。

## 21. 命令与消息路由

现有 `studio|动作|参数` 路由可继续复用，但后续应逐步从仅属于 Studio 的命名抽象为统一能力，例如：

```text
CDBoxCommandRouter
CDBoxWebBridge
CDBoxHostManager
```

要求：

- WinForms、侧栏、独立页和工作台调用同一业务命令；
- UI 只发送明确动作和参数；
- 业务规则不写在 JavaScript 点击事件中；
- 路由返回统一结果对象，包括成功、错误码、用户消息和诊断信息；
- 不同入口的行为必须一致。

## 22. 事件与实时联动规范

CDBox 中属性联动较多，但不得无节制地增加全局事件监听。

事件驱动功能必须考虑：

- 文档切换；
- 多文档环境；
- 对象被删除；
- 事务重入；
- 撤销和重做；
- 批量操作性能；
- 防抖；
- 插件卸载和事件解绑。

推荐：

- 界面字段内部联动可实时；
- 大范围图元刷新优先显式提交；
- 对象修改监听先记录“脏状态”，延迟检查；
- 不在每个对象变化事件中立即重算全图；
- 自动更新必须可关闭；
- 任何后台更新不得绕过 AutoCAD 撤销体系。

## 23. 撤销、事务和失败回滚

所有修改 DWG 的操作必须：

- 使用 AutoCAD 正确的文档锁和事务；
- 作为用户可理解的撤销步骤；
- 批量操作失败时尽可能整体回滚；
- 不留下半生成对象；
- 长任务报告进度；
- 支持取消时安全结束；
- 将单项失败汇总，而不是静默跳过。

涉及多个阶段的复杂命令，应明确提交边界，避免一个巨型事务导致 CAD 长时间锁死。

## 24. 配置、模板和版本迁移

专业软件需要可复用项目配置：

- 图层识别规则；
- 属性默认表；
- 标注样式；
- 精确位数；
- 结构层默认值；
- 工程量规则；
- 图框和出图规则。

要求：

- 配置可导入、导出；
- 默认配置可恢复；
- 用户配置不因更新被静默覆盖；
- 配置文件有版本号；
- 升级时执行迁移；
- 迁移失败保留原文件并记录日志。

---

# 第六部分：专业化功能发展方向

## 25. 对象中心化操作

长期目标：用户选择对象后，CDBox 自动提供该对象的完整上下文。

选择管线时可显示：

- 基础属性；
- 图层父属性、分类和标签；
- 起点、终点节点；
- 结构层；
- 关联标注；
- 工程量摘要；
- 检查问题；
- 相关操作。

选择节点时显示对应节点信息和关联管线。

这比让用户记忆多个功能入口更接近专业软件的使用方式。

## 26. 检查系统

检查系统是 CDBox 专业化的重要方向，优先级高于全自动排版。

建议分为：

### 26.1 几何检查

- 管线断开；
- 端点未连接节点；
- 重复或重叠管线；
- 异常短线；
- 非法自交；
- 高程或 Z 值异常。

### 26.2 属性检查

- 必填属性缺失；
- 图层、父属性、分类和标签不一致；
- 管径、材料或恢复类型异常；
- 节点类型和属性冲突；
- 结构层与深度联动不一致。

### 26.3 标注检查

- 缺少标注；
- 重复标注；
- 关联失效；
- 内容过期；
- 图面重叠；
- 格式不符合项目规则。

### 26.4 工程量检查

- 统计对象遗漏；
- 计算参数缺失；
- 公式输入异常；
- 结果与对象属性不一致；
- 特殊分类没有完整计算逻辑。

检查结果应支持：

- 严重、警告、建议等级；
- 点击定位和高亮；
- 单项修复；
- 批量修复；
- 忽略和说明；
- 导出报告。

## 27. 项目模板与工作流

不同项目应能加载不同的：

- 图层规则；
- 属性默认值；
- 结构层；
- 标注样式；
- 工程量规则；
- 出图规范。

长期可形成：

```text
项目模板
→ 打开图纸
→ 自动加载规则
→ 对象识别和属性编辑
→ 标注与断面
→ 工程量
→ 出图前检查
```

## 28. 批量操作能力

专业化不等于“一键完成所有事情”，而是对重复操作提供可靠批处理：

- 范围内批量刷新属性；
- 批量生成或更新标注；
- 批量生成断面；
- 批量检查；
- 批量修复确定性问题；
- 批量导出工程量。

批量操作必须提供预览、范围、进度、取消和结果汇总。

## 29. 诊断和维护能力

应逐步提供：

- 一键导出诊断包；
- CAD、CASS、CDBox 和 WebView2 版本信息；
- 最近命令和异常栈；
- 配置摘要；
- 更新日志；
- 元数据健康检查；
- 图纸中 CDBox 对象统计；
- 修复或迁移工具。

---

# 第七部分：Codex 修改现有代码的强制工作流程

## 30. 开始修改前必须调查

Codex 在任何影响现有功能的任务中，必须先：

1. 确认用户指定的最新源码基线；
2. 找到真实命令入口；
3. 找到业务服务、图元生成、预览、设置、配置和事务代码；
4. 查明功能之间的联动关系；
5. 查明是否已有可复用服务；
6. 查明旧 WinForms 和 WebView2 是否共用逻辑；
7. 列出将修改的文件和原因；
8. 说明是否会影响现有 DWG、配置和用户操作习惯。

不得通过类名猜测功能逻辑后直接新增一套实现。

## 31. 修改原则

Codex 必须：

- 复用现有稳定计算；
- 复用现有预览和落图逻辑；
- 复用现有设置；
- 复用已有命令路由；
- 保持旧功能兜底；
- 保持原有默认值和用户配置；
- 尽量只修改必要文件；
- 对公共规则建立单一来源；
- 避免同一业务公式复制到多个 UI 中。

## 32. 禁止事项

Codex 不得：

1. 为实现新界面而重新实现已有业务算法；
2. 未经确认删除旧 WinForms 入口；
3. 未经确认改变命令名；
4. 未经确认改变已有图层、文字类型、颜色、精度、图元类型和落图外观；
5. 引入私有 CAD 实体；
6. 将 WebView2 设为核心命令运行的必要条件；
7. 在打开旧图时静默批量写入或迁移全部对象；
8. 在属性变化事件中无条件扫描全图；
9. 静默覆盖用户手工修改内容；
10. 只修复界面表现而破坏独立页、内嵌页或旧版入口；
11. 忽略 AutoCAD 多文档、撤销、事务和文档锁；
12. 将异常吞掉并只返回“操作失败”。

## 33. 输出要求

Codex 完成任务后应提供：

- 修改文件列表；
- 每个文件的修改目的；
- 关键实现说明；
- 未修改但依赖的现有逻辑；
- 编译和测试步骤；
- 兼容性风险；
- 尚未完成或需要后续验证的事项。

当用户要求“仅给出修改文件”时，应遵守用户要求，但代码内部仍需保持上述标准。

---

# 第八部分：测试与验收规范

## 34. 基础功能回归

每次修改必须验证：

- 命令能正常启动和退出；
- `Esc` 行为正确；
- 连续操作行为未改变；
- 预览和实际落图一致；
- 字体、文字高度、颜色、精度未意外变化；
- 图层和属性识别正常；
- 撤销和重做正常；
- 保存、关闭、重新打开后结果正常。

## 35. 无 CDBox 环境兼容测试

涉及新图形或元数据时必须验证：

- 普通 AutoCAD 可打开；
- 不出现代理对象依赖；
- 图元可修改；
- 可打印；
- 可复制；
- 可解除组；
- 附加数据损坏不影响图形显示。

## 36. 图纸交换测试

重点验证：

- 同图复制；
- 跨图复制；
- COPYCLIP / PASTECLIP；
- WBLOCK；
- INSERT；
- SAVEAS；
- PURGE；
- AUDIT / RECOVER；
- XREF；
- 对象分割、合并和删除；
- 标注子图元被单独删除或修改。

复制后若产生重复 CDBox ID，应检测并重新分配，不得让关联悄然指向错误对象。

## 37. 版本与运行环境

优先支持并测试：

- AutoCAD 2023；
- CASS 11；
- 当前项目已经支持的更早 AutoCAD 版本；
- 无 WebView2 Runtime 时的核心命令；
- Studio 页面加载失败时的旧版或命令行兜底。

## 38. 性能测试

批量和实时功能应至少测试：

- 少量对象；
- 数百对象；
- 大型复杂图；
- 多次连续执行；
- 文档切换；
- 取消和异常中断。

不得只在空白测试图上验证。

---

# 第九部分：推荐实施路线

## 39. 近期阶段

### 阶段 A：工作台职责调整

1. 大型工作台默认不自动打开；
2. 记忆窗口状态；
3. 建立紧凑停靠侧栏；
4. 侧栏接入当前对象摘要、动态快捷操作、收藏和问题摘要；
5. 复杂页面继续按需独立打开；
6. 核心命令不依赖工作台存在。

### 阶段 B：管线长度标注对象化验证

1. 保持现有外观和预览逻辑不变；
2. 写入 `AnnotationId`；
3. 为源管线写入或复用稳定 `CDBoxObjectId`；
4. 使用 AutoCAD 原生 Group；
5. 增加整体移动命令；
6. 增加关联诊断命令；
7. 验证普通 CAD 可编辑。

### 阶段 C：标注编辑和检查

1. 单标注刷新；
2. 范围刷新；
3. 手工覆盖规则；
4. 关联丢失检查；
5. Group 修复；
6. 侧栏或上下文面板快速编辑。

### 阶段 D：统一对象模型和检查中心

1. 统一管线、节点、标注领域模型；
2. 提取共享属性和联动规则；
3. 建立几何、属性、标注检查；
4. Studio 转为项目和检查管理中心；
5. 点击问题可定位到 CAD。

### 阶段 E：有限智能整理与 AI 辅助

1. 重叠检测；
2. 固定规则对齐和排列；
3. 候选位置；
4. AI 汇总检查结果；
5. 自然语言调用确定性功能。

---

# 第十部分：最终设计结论

CDBox 后续专业化不应依赖一个越来越大的工作台，也不应通过创造封闭对象替代 CAD。

推荐的最终形态：

```text
                         CDBox
                           │
        ┌──────────────────┼──────────────────┐
        │                  │                  │
   原生 CAD 图形       CDBox 语义数据      专业业务能力
        │                  │                  │
可显示、可编辑、可交付   对象关联、版本      属性、标注、工程量、检查
        └──────────────────┼──────────────────┘
                           │
                    标准 DWG 文件
```

界面形态：

```text
CAD 命令：立即执行
紧凑侧栏：当前对象和高频操作
上下文面板：临时快速调整
独立页面：复杂业务
大型工作台：项目、检查、设置和管理中心
```

产品能力：

```text
对象识别
→ 属性和关联
→ 标注与断面
→ 工程量
→ 修改和刷新
→ 检查与定位
→ 可交付 DWG
```

最终原则可概括为：

> **保持 CAD 原生、围绕对象组织、以确定性规则保证正确、以智能辅助提升效率、以检查和维护形成专业闭环。**

---

# 第十一部分：规范维护规则

## 40. 后续更新方式

本聊天作为 CDBox 设计规范的持续维护线程。后续形成新结论时，应：

1. 判断是补充、修改还是废止现有条款；
2. 更新对应章节，而不是简单追加互相冲突的段落；
3. 修改文档版本号；
4. 在变更记录中写明日期、内容和影响范围；
5. 若新结论改变既有开发路线，应明确标注替代关系；
6. 输出更新后的完整主文档，避免 Codex 同时读取多个冲突文件。

## 41. 变更状态标记

必要时可使用：

- **强制**：所有后续开发必须遵守；
- **推荐**：默认采用，特殊情况可说明后调整；
- **实验**：仅用于 Preview 或验证分支，不得影响稳定版；
- **暂缓**：方向保留，但近期不投入；
- **废止**：旧规范不再适用。

---

# 变更记录

## 1.0.0 — 2026-07-15

首次建立统一主规范，归纳并整合：

- 不追求近期全自动标注和完美避让；
- 建立标准图元、原生 Group 和附加元数据组成的智能标注；
- 标注与源对象绑定，用于整体调整、刷新和检查；
- 普通 CAD 中必须正常显示和修改；
- 禁止私有 CAD 对象和数据库强依赖；
- 从功能集合转向对象全生命周期；
- 建立统一对象模型和代码分层；
- 大型工作台改为管理中心；
- 日常操作采用紧凑侧栏、命令、上下文面板和按需独立页；
- 明确 Codex 修改现有代码的调查、复用、禁止和输出规范；
- 明确后续在本聊天中持续更新本文档。
