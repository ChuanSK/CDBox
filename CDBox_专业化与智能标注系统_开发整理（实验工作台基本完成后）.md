# CDBox 专业化与智能标注系统开发整理

> 用途：作为后续通过 Codex 继续开发 CDBox 的设计依据、实现约束和任务拆分参考。
> 当前背景：CDBox 面向 AutoCAD 2023 + CASS 11，尽量兼容更早版本；现有稳定功能包括图层管理、属性体系、管线/节点/表面积标注、断面图、工程量统计以及 WebView2 Studio 实验分支。

---

## 1. 本次讨论的核心结论

### 1.1 暂不追求“全自动标注与完美避让”

实际管线图中，主管、支管、节点和既有地物高度密集，标注位置不仅受到几何碰撞影响，还受到制图习惯、引线方向、道路边界、相邻管线、图面留白、出图比例等约束。

因此，传统的“为每个标注自动寻找最佳位置”会迅速演变成复杂的二维排版和地图标签布局问题，开发成本高、结果仍需人工调整。

当前更合理的目标不是替代人工排版，而是：

> **让人工调整从“分别拖文字、拖引线或删除重做”，升级为“选择一个完整标注，整体移动、快速编辑和自动刷新”。**

### 1.2 标注系统的优先优化方向

将现有由文字、横线、斜引线等多个图元构成的标注，升级为一个由 CDBox 管理的“逻辑整体”：

- 用户选中或拖动时可整体移动；
- 引线、横线和文字保持正确关系；
- 双击或执行编辑命令时进入专用编辑流程；
- 标注与被标注对象建立稳定关联；
- 为后续标注检查、批量刷新和属性同步提供数据基础。

### 1.3 原生 CAD 兼容必须作为最高级原则

CDBox 不应走“私有 CAD 对象”或 ObjectARX 自定义实体路线。

所有 CDBox 生成的对象必须满足：

- 在未安装 CDBox 的普通 AutoCAD/CASS 中正常显示；
- 不出现代理对象、空白对象或必须安装对象使能器的情况；
- 可以选择、移动、删除、编辑和打印；
- 复制、粘贴、另存、导出和图纸交换尽量保持正常；
- CDBox 的智能能力仅作为标准图元上的附加数据存在。

CDBox 的产品原则应明确为：

> **CDBox 可以增强 CAD，但不能绑架 CAD。**

---

## 2. 当前标注流程的主要痛点

目前管线长度标注等功能虽然已经具备较好的预览能力，但生成后通常由多个独立图元组成。遇到复杂图面时，用户常常需要：

1. 先拖动文字；
2. 再单独调整横线；
3. 再调整斜引线；
4. 检查文字与引线是否错位；
5. 调整失败时直接删除并重新标注。

在支管密集区域，一个节点附近可能同时存在多条短支管、多种管径和多种恢复类型，标注经常需要反复微调。真正影响效率的不是生成标注，而是生成后的维护、整理和属性变化后的更新。

---

## 3. 推荐的总体技术路线

### 3.1 标准图元 + 原生分组 + 附加元数据

建议优先采用以下组合：

1. **标准 AutoCAD 图元**
   - 文字继续使用当前实际实现中的原生文字实体，例如 `DBText`，是否使用 `MText`必须以现有源码和稳定效果为准；
   - 引线和横线继续使用 `Line`、`Polyline`、`Leader` 或现有稳定实现；
   - 不创建 CDBox 私有实体。

2. **AutoCAD 原生 Group 作为逻辑整体的首选方案**
   - 将同一标注中的文字、横线和引线加入同一原生 `Group`；
   - 在普通 CAD 中仍是标准图元；
   - 开启组选择时可整体选择和移动；
   - 用户仍可在普通 CAD 中解除组、编辑单个图元或重新组合；
   - 比自定义实体更兼容，也比直接做成普通块更方便单独编辑文字。

3. **XData 或 Extension Dictionary 保存 CDBox 语义数据**
   - 不依赖外部数据库才能显示图纸；
   - 元数据损坏或缺失时，图形本身仍可正常使用；
   - CDBox 加载后可恢复关联、刷新和检查能力。

> 最终选择 `Group`、块参照或其他原生组合方式前，Codex 必须先检查最新源码中三类标注的实际图元组成和现有编辑逻辑。原则不变：不得引入私有代理对象。

### 3.2 不仅依赖 Handle，建立稳定 CDBox ID

仅保存 AutoCAD Handle 不足以支撑长期关联，因为复制、分割、合并、WBLOCK、INSERT 和跨图复制都可能改变对象关系或产生重复。

建议为可管理对象写入稳定 ID：

```text
CDBoxObjectId = GUID
CDBoxObjectType = Pipe / Node / Area / Annotation
MetadataVersion = 1
```

被标注对象：

```text
CDBoxObjectId: PIPE-...
CDBoxObjectType: Pipe
```

标注逻辑整体：

```text
AnnotationId: ANN-...
AnnotationType: PipeLength / Node / SurfaceArea
SourceObjectIds: [PIPE-...]
GroupName或Group标识: ...
MetadataVersion: 1
```

标注中的每个子图元也应保存最小关联信息：

```text
AnnotationId: ANN-...
AnnotationPart: MainText / SecondaryText / LandingLine / LeaderLine
```

这样即使原生 Group 被用户解除，CDBox 仍有机会通过 `AnnotationId` 找回全部组成部分并修复。

### 3.3 DWG 是图形和基础数据的最终保障

后续可以考虑项目数据库或 SQLite，但不能让外部数据库成为图纸正常显示和基础编辑的必要条件。

推荐原则：

- DWG 内保留几何、文字和最关键关联数据；
- 外部项目数据库只用于可重建的缓存、统计、历史记录或跨图项目管理；
- 外部数据丢失时，可通过 DWG 扫描重建主要索引；
- 不允许出现“没有数据库就无法打开或无法编辑图纸”的情况。

---

## 4. 智能标注对象的目标体验

### 4.1 整体选择和整体移动

用户选择标注中的任一部分时，CDBox 可识别其所属 `AnnotationId`，并提供整体操作：

- 整体移动；
- 保持文字、横线、引线之间的相对关系；
- 引线端点继续吸附或关联原对象；
- 移动完成后重新计算必要的横线长度和文字布局。

普通 CAD 环境中，原生 Group 仍可提供基本的整体移动能力；即使组选择关闭，用户仍可正常编辑各标准图元。

### 4.2 专用编辑入口

可提供以下一种或多种入口：

- 双击标注；
- 右键菜单“编辑 CDBox 标注”；
- 命令选择标注进入编辑；
- Studio 中选择对象后显示标注信息。

初期建议优先实现稳定的显式命令，双击事件可后续增加，避免一开始引入复杂事件拦截和兼容问题。

编辑内容可包括：

- 重新选择关联对象；
- 切换引线方向；
- 调整文字位置；
- 切换是否显示开挖信息；
- 调整精确位数；
- 恢复自动生成内容；
- 保留或清除用户手工修改。

### 4.3 属性变化后的刷新

当管径、恢复类型、长度、深度、节点信息等发生变化时，提供：

- 单个标注刷新；
- 选择范围刷新；
- 全图 CDBox 标注刷新；
- 出图前检查并提示内容不一致。

初期不建议立刻实现“所有对象变化时实时自动更新”，原因包括：

- AutoCAD 事件和事务嵌套复杂；
- 容易破坏撤销/重做；
- 大图中可能产生性能问题；
- 用户手工修改内容可能被意外覆盖。

第一阶段更稳妥的方案是：

> **显式刷新 + 检查提示，后续再增加可配置的延迟同步或事件驱动更新。**

### 4.4 手工修改与自动值的冲突规则

需要明确区分：

- 自动生成值；
- 用户手工覆盖值；
- 当前被标注对象的真实属性值。

建议保存以下状态：

```text
ContentMode = Auto / ManualOverride
LastGeneratedSignature = ...
LastSourceSignature = ...
```

刷新时：

- `Auto`：可直接重新生成；
- `ManualOverride`：先提示差异，用户选择保留手工内容或恢复自动；
- 不允许静默覆盖用户明确修改的文字。

---

## 5. 标注检查系统

标注对象化之后，最有价值的下一步不是全自动避让，而是建立检查系统。

### 5.1 基础关联检查

- 管线存在，但没有长度标注；
- 节点存在，但没有节点标注；
- 标注关联对象已删除；
- 标注关联到错误类型的对象；
- 同一对象存在重复标注；
- Group 被破坏或子图元缺失；
- 子图元的 `AnnotationId` 不一致；
- 复制图形后出现重复 `CDBoxObjectId`。

### 5.2 内容一致性检查

- 标注长度与当前几何长度不一致；
- 管径、材料、恢复类型与属性表不一致；
- 节点编号、井深、井类型与对象属性不一致；
- 精确位数或单位格式不符合当前设置；
- 标注内容仍使用旧属性。

### 5.3 图面检查

后续可增加：

- 文字框互相重叠；
- 文字压住管线或节点；
- 引线穿越其他文字；
- 引线过长；
- 多个标注间距过小。

图面检查的第一目标是“发现问题并定位”，而不是立即自动移动全部标注。

### 5.4 检查结果呈现

Studio 中可按严重程度展示：

```text
严重：关联对象丢失、图元不完整、重复 ID
警告：内容与属性不一致、重复标注、缺少标注
建议：文字重叠、引线过长、布局不整齐
```

用户点击问题后，CAD 自动缩放并高亮对应位置。

---

## 6. CDBox 专业化的主要方向

### 6.1 从“命令集合”转为“对象全生命周期”

专业化的关键不是继续增加零散命令，而是让一条管线或一个节点贯穿完整工作流：

```text
创建/识别
→ 属性编辑
→ 节点关联
→ 标注
→ 断面
→ 工程量
→ 修改
→ 刷新
→ 检查
→ 出图
```

所有模块应围绕同一对象模型工作，避免各自重复识别和保存同类数据。

### 6.2 对象中心化的 Studio

未来 Studio 不应只按功能卡片组织，也应支持“选择对象后显示完整上下文”。

选择一条管线后可展示：

- 基础属性；
- 起点和终点节点；
- 图层父属性、分类和标签；
- 关联标注；
- 结构层；
- 工程量；
- 检查问题；
- 快捷操作。

这会比用户在多个独立窗口和命令之间切换更接近专业软件体验。

### 6.3 统一对象模型

建议逐步建立独立于 UI 和 AutoCAD 图元细节的领域模型，例如：

```text
CDPipe
- Id
- CadEntityId / Handle
- Geometry
- ParentProperty
- Category
- Tags
- Diameter
- Material
- StartNodeId
- EndNodeId
- StructureLayers
- AnnotationIds

CDNode
- Id
- NodeType
- Number
- Depth
- ConnectedPipeIds
- AnnotationIds

CDAnnotation
- Id
- Type
- SourceObjectIds
- PartEntityIds
- GroupId
- ContentMode
- Style
- MetadataVersion
```

领域模型不应直接暴露在 WinForms/WebView2 页面中，也不应让业务逻辑到处直接操作 `Polyline`、`DBText` 等 AutoCAD 类。

### 6.4 代码分层建议

```text
CDBox.Core
- 配置
- 日志
- 版本
- 通用结果和错误模型
- ID 与元数据版本

CDBox.Cad
- AutoCAD API 封装
- 事务管理
- 选择集
- 图元读写
- Group 管理
- XData/Extension Dictionary
- 高亮与定位

CDBox.Domain
- Pipe / Node / Annotation 等领域模型
- 纯业务规则
- 不直接依赖 UI

CDBox.Metadata
- CDBox ID
- 关联数据读写
- 数据迁移
- 重复 ID 检测和修复

CDBox.Annotation
- 标注创建
- 标注编辑
- 标注整体移动
- 标注刷新
- 标注检查
- 各标注类型策略

CDBox.Quantity
- 工程量计算

CDBox.Validation
- 几何、属性、标注和出图检查

CDBox.UI.WinForms
- 旧界面和兜底界面

CDBox.Studio
- WebView2 页面
- 对象面板
- 检查中心

CDBox.Update
- 更新检查、下载、安装和诊断
```

实际工程不必一次性拆成大量程序集，可先通过命名空间和目录完成逻辑分层，避免重构范围过大。

### 6.5 CAD API 隔离

业务模块不应在大量位置直接创建事务、访问数据库和修改图元。

应逐步提供封装接口，例如：

```csharp
IAnnotationEntityStore
IObjectMetadataStore
ICadSelectionService
ICadHighlightService
ICadTransactionRunner
```

这样可以：

- 降低 AutoCAD 版本升级影响；
- 更容易测试业务逻辑；
- 防止事务使用方式不一致；
- 为未来兼容其他 CAD 平台保留可能性。

### 6.6 模板和项目配置

专业软件需要减少每个项目重复设置：

- 图层识别规则；
- 属性默认表；
- 标注样式；
- 精确位数；
- 结构层默认值；
- 工程量规则；
- 图框和出图规则。

建议最终形成可导入、导出和版本化的项目模板，同时继续保持默认配置可恢复。

### 6.7 稳定性和诊断

应逐步完善：

- 所有批量操作使用统一事务和失败回滚；
- 长任务有进度、取消和错误汇总；
- 日志包含命令、CAD 版本、CASS 环境、操作对象数量和异常栈；
- 支持一键导出诊断包；
- 更新失败可恢复到旧版本；
- 配置升级有版本迁移，不直接覆盖用户数据。

### 6.8 AI 的合理定位

AI 应作为分析和交互层，而不是基础几何和数据一致性的唯一执行者。

适合 AI 的工作：

- 将自然语言转为明确的检查或批处理命令；
- 汇总检查结果；
- 解释异常原因；
- 根据规则推荐修复方案；
- 帮助生成项目模板和识别规则。

不适合早期直接交给 AI 的工作：

- 决定所有标注的最终排版位置；
- 在没有确定性检查和撤销机制的情况下大规模修改图纸；
- 替代长度、连接关系和工程量等确定性算法。

---

## 7. 建议实施阶段

### 阶段 0：源码调查与设计确认

Codex 开始修改前必须先完成：

- 确认最新可用源码基线；
- 查明管长、节点、表面积标注实际生成了哪些图元；
- 查明当前预览和落图共用程度；
- 查明是否已有 XData、扩展字典、对象属性 ID 或 Group 使用；
- 查明命令入口、设置读取和撤销事务结构；
- 输出最小改动方案，不直接重写稳定标注算法。

### 阶段 1：仅改造管线长度标注

目标：验证标注对象化方案。

实现：

- 新标注生成稳定 `AnnotationId`；
- 被标注管线拥有稳定 `CDBoxObjectId`；
- 标注子图元写入关联元数据；
- 创建原生 Group；
- 增加选择标注并整体移动的命令；
- 增加读取并定位关联管线的能力；
- 普通 CAD 中仍可正常显示和编辑。

暂不实现：

- 全图实时监听；
- 自动避让；
- 大规模架构重写；
- 所有标注类型一次性迁移。

### 阶段 2：标注编辑和刷新

- 单个标注编辑；
- 重新选择源对象；
- 自动/手工内容模式；
- 单个和范围刷新；
- 关联丢失提示；
- Group 破坏后的修复。

### 阶段 3：统一三类标注

将相同机制扩展到：

- 节点标注；
- 表面积标注；
- 管线长度标注。

建立统一 `AnnotationManager`，各类型通过策略或处理器实现差异逻辑。

### 阶段 4：标注检查中心

- 缺失标注；
- 重复标注；
- 无效关联；
- 内容不一致；
- 重复 ID；
- 子图元缺失；
- 问题定位和批量修复。

### 阶段 5：有限的智能整理

只做确定性较高的辅助：

- 检测重叠；
- 相同方向的标注对齐；
- 固定间距排列；
- 支管标注按组排列；
- 给出候选位置，不强制全自动排版。

---

## 8. 兼容性验收标准

每个阶段必须至少验证：

### 8.1 无 CDBox 环境

- 图纸正常打开；
- 不出现代理实体警告；
- 所有文字、线、填充、块等正常显示；
- 可选择、移动、删除和编辑；
- 可正常打印和导出 PDF；
- Group 即使失效或被解除，组成图元仍正常。

### 8.2 有 CDBox 环境

- 能重新识别标注整体；
- 能找到关联对象；
- 标注移动后引线和文字关系正确；
- 刷新内容不破坏用户明确的手工覆盖；
- 撤销和重做行为正确；
- 保存、关闭、重新打开后关联仍存在。

### 8.3 图纸交换

重点测试：

- 复制/粘贴；
- 同图复制；
- 跨图复制；
- WBLOCK；
- INSERT；
- SAVEAS；
- PURGE；
- AUDIT/RECOVER；
- XREF 场景；
- 被标注对象分割、合并和删除；
- 标注子图元被单独删除或修改。

需要为复制后重复 ID 设计检测和重新分配机制。

### 8.4 版本环境

优先测试：

- AutoCAD 2023；
- CASS 11；
- 当前项目已支持的更早 AutoCAD 版本；
- WebView2 Studio 未加载时，核心 CAD 标注功能仍可工作。

---

## 9. Codex 开发约束

后续将本文档提供给 Codex 时，应同时强调：

1. 必须以用户指定的最新可用源码为基线；
2. 先阅读并复用现有标注生成、预览、设置、属性识别和事务逻辑；
3. 不得重写已经稳定的预览与落图算法，除非确认无法复用；
4. 不得引入私有 CAD 实体、代理对象或必须安装 CDBox 才能显示的对象；
5. 不得将外部数据库设为显示和基础编辑的必要条件；
6. 所有写操作必须支持 AutoCAD 撤销；
7. 新元数据必须带版本号并支持后续迁移；
8. 对旧图纸只做识别和可选迁移，不在打开图纸时静默批量修改；
9. 每次只实现一个小阶段，优先提供修改文件和变更说明；
10. 编译前检查 AutoCAD/CASS 引用冲突，避免 `Font`、`FlowDirection` 等命名歧义；
11. 保持旧 WinForms 功能或旧命令作为兜底，除非用户明确要求删除；
12. 所有功能在没有 WebView2 Runtime 或 Studio 页面异常时，不应影响核心 CAD 命令。

---

## 10. 第一项推荐开发任务

建议下一次实际开发从以下任务开始：

> **“管线长度标注对象化第一阶段”**

任务范围：

- 保持现有管长标注外观、文字类型、字体、颜色、精度和预览行为不变；
- 生成后为各子图元写入统一 `AnnotationId`；
- 为被标注管线写入或复用稳定 `CDBoxObjectId`；
- 使用 AutoCAD 原生 Group 将文字、横线和引线组成逻辑整体；
- 增加一个命令：选择任一标注子图元后，识别完整标注并整体移动；
- 增加一个诊断命令：显示标注 ID、类型、组成图元和关联管线；
- 验证无 CDBox 的 CAD 中可以正常显示、移动、解组和编辑；
- 暂不增加自动避让、双击拦截、实时事件更新和复杂 UI。

这项任务可以用最小风险验证整个方向，同时不会破坏现有稳定功能。

---

## 11. 最终产品方向

CDBox 的理想定位不是“只能在自身体系内工作的专业软件”，也不只是“很多命令组成的工具箱”，而是：

> **一个基于标准 DWG 和原生 CAD 图元、为管线测绘提供对象关联、属性管理、标注、工程量、检查和智能辅助的增强层。**

其核心竞争力应包括：

- 不锁定图纸；
- 不破坏原生 CAD 兼容性；
- 生成结果可交付、可编辑、可长期保存；
- 对象属性、标注和工程量保持一致；
- 用户仍掌握最终制图决定；
- 插件负责减少重复劳动、发现错误和维护关联。
