# CDBox 发布版本约定

- 用户指定的起始版本为 5.1.3；最近一次交付为 5.1.9。下一轮未指定版本时使用 5.1.10。
- 此后每轮新的变更交付，若用户没有特别指定版本，默认将当前产品版本的补丁号加一，例如 5.1.3 → 5.1.4。
- 同一轮实现、修正、验证和重复编译使用同一个版本，不因构建次数重复递增。
- 同步主程序、安装器、安装清单和更新元数据的产品版本及数字版本码；业务模块自身的接口版本按其兼容性单独管理。
- 构建并验证对应安装器。已有版本目录中不同内容的安装器不可覆盖。
