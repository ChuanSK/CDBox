## 未来可期

### 一、工程量一览

实时统计当前工程图纸（或固定区域框内）工程量，以现代化网页动态图表的方式呈现。

1. 检查/沉泥井类

   ```
   检查/沉泥井工程量
   ```

2. 主管类

   ```
   主管工程量
   ```

3. 支管类

   ```
   支管工程量
   ```

4. 其他类别（315井、化粪池等）

   ```
   其他类别工程量
   ```

### 二、次时代界面优化

1. 功能卡片可随意拖拽；
2. 表格中条目可随意拖拽；
3.

## 修改建议4.1

基于当前 CDBox Studio Preview 4.1，继续优化属性识别表独立 WebView2 窗口：独立模式删除顶部标题卡片，只保留规则表主体卡片；将“打开旧版属性识别表”和“关闭”按钮迁移到规则表卡片右上角；隐藏独立模式搜索栏，不彻底删除搜索能力，改成隐藏式 Ctrl + F 打开搜索，在规则表右上角放一个小搜索图标🔍，平时不显示大搜索框，点击后再展开；表格区域下方增加规则表填写说明提示（特别是匹配方式和标签）；独立窗口改为无系统外框的自定义圆角窗口，自定义标题栏空白区域支持拖动窗口（窗口整体白色圆角，外侧轻微阴影，内部 1px 浅蓝灰边框），去掉系统外框后补上窗口边缘拖拽，增加自定义最小化和关闭按钮，关闭按钮悬停时使用红色高亮。

统一圆角：

窗口圆角：16px
规则表卡片圆角：18px
按钮圆角：12px
输入框 / 下拉框：10px
表格行内输入框：8px

按钮：

保存：主按钮，蓝紫渐变
恢复默认：警告按钮，橙色描边或浅橙背景
删除：危险按钮，红色描边
打开旧版：普通按钮
关闭：暗红色描边，悬停高亮

适当增加独立页面打开时宽度以适应表格区域宽度，保留工作台内嵌页面现有布局，不删除旧 WinForms 属性识别表。

┌──────────────────────────────────────────────┐
│ 属性识别表                                                                                                                               —   ×  │
├──────────────────────────────────────────────┤
│ 规则表                                                                                                                                                       │
│ 从上到下匹配，启用的规则才参与自动识别。                                                                │
│                                                                                                                                                                       │
│ [新增] [删除] [上移] [下移] [恢复默认] [保存]                        [打开旧版] [关闭] │
│                                                                                                                                                                       │
│ 表格区域                                                                                                                                                  │
└──────────────────────────────────────────────┘

## 修改建议4.2

1. 似乎为了实现窗口边缘拖拽自定义标题栏外层多套了一层，尽量不要这一层来实现窗口边缘拖拽，或者隐藏这一层；
2. “打开旧版”和“关闭”按钮与搜索框上下调换位置，搜索框展开改为图标在右侧，实际搜索框在左侧；
3. 将“关闭”按钮的效果改为与“删除”按钮一样。
